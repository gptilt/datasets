[project]
name = "ds-models"
version = "0.1.0"
dependencies = [
    "transformers",
    "accelerate",
    "torch",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_models"]
