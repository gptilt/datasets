[project]
name = "ds-common"
version = "0.1.0"
dependencies = [
    "pyarrow",
    "pyiceberg",
    "tqdm>=4.65.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_common"]