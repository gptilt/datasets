[project]
name = "ds-documents"
version = "0.1.0"
dependencies = [
    "polars",
    "pyiceberg",
    "ds_esports",
    "ds_storage",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_documents"]
