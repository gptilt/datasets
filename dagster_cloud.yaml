locations:
  - location_name: datasets
    code_source:
      package_name: orchestration.definitions
    build:
      directory: ./
      registry: ghcr.io/gptilt/datasets
    run_coordinator:
      module: dagster.core.run_coordinator
      class: QueuedRunCoordinator
      config:
        max_concurrent_runs: 5