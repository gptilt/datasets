[project]
name = "ds-scribe"
version = "0.1.0"
dependencies = [
    "torch",
    "whisperx",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_scribe"]