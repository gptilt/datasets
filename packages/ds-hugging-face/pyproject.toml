[project]
name = "ds-hugging-face"
version = "0.1.0"
dependencies = [
    "datasets>=3.6.0",
    "huggingface-hub>=0.30.2",
    "Jinja2>=3.1.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_hugging_face"]