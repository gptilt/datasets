[project]
name = "ds-youtube"
version = "0.1.0"
dependencies = [
    "yt-dlp>=2026.2.4"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_youtube"]