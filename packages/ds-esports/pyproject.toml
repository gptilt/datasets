[project]
name = "ds-esports"
version = "0.1.0"
dependencies = [
    "polars",
    "pyarrow",
    "pyiceberg",
    "mwcleric",
    "ds_common",
    "ds_storage",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_esports"]
