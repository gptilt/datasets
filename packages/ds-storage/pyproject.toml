[project]
name = "ds-storage"
version = "0.1.0"
dependencies = [
    "boto3>=1.40.32",
    "polars>=1.30.0",
    "pyarrow>=19.0.1",
    "pyiceberg>=0.11.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_storage"]