[project]
name = "ds-cdragon"
version = "0.1.0"
dependencies = [
    "requests",
    "tqdm>=4.65.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ds_cdragon"]